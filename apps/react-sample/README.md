# React Sample (Local Workspace)

Runnable sample for the `@lexion-rte/react` adapter.

## Run

From repository root:

```bash
pnpm install
pnpm dev:react-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/react-sample dev
```
