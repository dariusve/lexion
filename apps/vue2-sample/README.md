# Vue2 Adapter Sample (Local Workspace)

Framework-native Vue 2 sample for `@lexion-rte/vue2`.

## Run

From repository root:

```bash
pnpm install
pnpm dev:vue2-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/vue2-sample dev
```
