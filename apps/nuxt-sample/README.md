# Nuxt Adapter Sample (Local Workspace)

Framework-native Nuxt sample for `@lexion-rte/nuxt`.

## Run

From repository root:

```bash
pnpm install
pnpm dev:nuxt-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/nuxt-sample dev
```
