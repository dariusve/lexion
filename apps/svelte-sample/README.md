# Svelte Adapter Sample (Local Workspace)

Framework-native Svelte sample for the `@lexion-rte/svelte` action adapter.

## Run

From repository root:

```bash
pnpm install
pnpm dev:svelte-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/svelte-sample dev
```
