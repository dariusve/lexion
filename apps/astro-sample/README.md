# Astro Adapter Sample (Local Workspace)

Framework-native Astro sample for `@lexion-rte/astro`.

## Run

From repository root:

```bash
pnpm install
pnpm dev:astro-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/astro-sample dev
```
