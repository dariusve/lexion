# Next Adapter Sample (Local Workspace)

Framework-native Next.js sample for `@lexion-rte/next` (App Router).

## Run

From repository root:

```bash
pnpm install
pnpm dev:next-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/next-sample dev
```
