# Web Sample (Local Workspace)

Runnable sample for the `@lexion-rte/web` adapter.

## Run

From repository root:

```bash
pnpm install
pnpm dev:web-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/web-sample dev
```
