# Angular Adapter Sample (Local Workspace)

Framework-native Angular sample using standalone components and `@lexion-rte/angular`.

## Run

From repository root:

```bash
pnpm install
pnpm dev:angular-sample
```

Or directly:

```bash
pnpm --filter @lexion-rte/angular-sample dev
```
